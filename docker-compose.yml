version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: rachaelwilliams-redirect
    ports:
      - "8080:8123"
    volumes:
      - ./nginx.conf.template:/etc/nginx/conf.d/redirect.conf:ro
    restart: unless-stopped 